@PACKAGE_INIT@

if (NOT TARGET stbipp)
  include ("${CMAKE_CURRENT_LIST_DIR}/StbippTarget.cmake")
endif ()

set (STBIPP_FOUND 1)

set (STBIPP_LIBRARY_PATH "@STBIPP_LIBRARY_PATH@")
set (STBIPP_INCLUDE_DIR  "@STBIPP_INCLUDE_DIR@")
set (STBIPP_ROOT_DIR     "@STBIPP_ROOT_DIR@")

set (STBIPP_VERSION_MAJOR  "@PROJECT_VERSION_MAJOR@")
set (STBIPP_VERSION_MINOR  "@PROJECT_VERSION_MINOR@")
set (STBIPP_VERSION_PATCH  "@PROJECT_VERSION_PATCH@")
