#pragma once
#if defined(@LIB_NAME@_EXPORT) && defined(@LIB_NAME@_SHARED) && defined(WIN32)
    #define @LIB_SYMBOL_NAME@ __declspec(dllexport)
#elif defined(@LIB_NAME@_SHARED) && defined(WIN32)
    #define @LIB_SYMBOL_NAME@ __declspec(dllimport)
#else
    #define @LIB_SYMBOL_NAME@
#endif
